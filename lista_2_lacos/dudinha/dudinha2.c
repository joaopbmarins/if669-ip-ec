#include <stdio.h>

int main(){
    long int n,k_q;
    int p, q;

    int p_calculado = 0, primo = 1;

    scanf("%ld %ld %d", &n, &k_q, &p);

    for(int i=n,num=n;i<k_q;i++){
        
    }

    return 0;
}